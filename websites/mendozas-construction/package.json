{
  "name": "mendozas-construction",
  "version": "1.0.0",
  "scripts": {
    "start": "mf-cra start",
    "build": "mf-cra build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "npx wrangler pages deploy build --project-name=mendozas-construction"
  },
  "dependencies": {
    "mf-cra": "^0.0.5"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
